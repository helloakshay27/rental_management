name: Post-merge Summary

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  summary:
    uses: helloakshay27/ci-workflows/.github/workflows/post-merge-summary.yml@v1
    secrets: inherit
