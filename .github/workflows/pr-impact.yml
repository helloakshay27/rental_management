name: PR Impact

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

jobs:
  impact:
    uses: helloakshay27/ci-workflows/.github/workflows/pr-impact.yml@v1
    secrets: inherit
